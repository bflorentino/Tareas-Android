<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ViewComidaActivity"
    android:orientation="vertical"
    android:paddingHorizontal="10dp">

    <ImageView
        android:layout_height="300dp"
        android:layout_width="300dp"
        android:id="@+id/verImagenValorTipo"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:text="Nombre" />

    <EditText
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:id="@+id/verNombreText"
        android:hint="Título de la Nota"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Tipo de Comida" />

    <EditText
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:id="@+id/verTipoText"
        android:hint="Cuerpo de la Nota"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="10dp">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Editar"
            android:id="@+id/btnEditar"
            android:textSize="12dp"
            android:onClick="onClickUpdateNota"
            android:backgroundTint="@color/green"
            />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Eliminar"
            android:id="@+id/btnEliminar"
            android:layout_marginLeft="10dp"
            android:textSize="12dp"
            android:onClick="onClickDeleteNota"
            android:backgroundTint="@color/red"
            />
    </LinearLayout>

</LinearLayout>